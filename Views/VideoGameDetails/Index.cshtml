@model IEnumerable<NETD3202Lab5_1.Models.VideoGameDetails>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.videoGameDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.videoGameDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.videoGameID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.videoGameID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.videoGameID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
